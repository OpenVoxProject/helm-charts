# -- Container image repository
image:
  # -- Image repository
  repository: ghcr.io/voxpupuli/openvoxview
  # -- Image tag
  tag: v1.4.0
  # -- Image pull policy
  pullPolicy: IfNotPresent

# -- Image pull secrets for private registries
imagePullSecrets: []

# -- Number of replicas
replicaCount: 1

# -- Container port for OpenVox View
port: 5000

# -- PuppetDB / OpenVox DB connection settings
puppetdb:
  # -- PuppetDB URL
  url: "https://puppetdb:8081"
  # -- Existing TLS secret name containing cert.pem and key.pem for client certificate authentication
  tlsSecretName: ""
  # -- Existing secret name containing the CA certificate (ca.pem)
  caSecretName: ""

# -- Additional environment variables as key-value pairs
extraEnv: {}
#  OPENVOXVIEW_DEFAULT_ENVIRONMENT: production

# -- Name of an existing secret to use as additional environment variables
extraEnvSecret: ""

# -- Container resource requests and limits
resources: {}
#  requests:
#    memory: 256Mi
#    cpu: 200m
#  limits:
#    memory: 512Mi
#    cpu: 500m

# -- Container security context
securityContext:
  runAsNonRoot: true
  allowPrivilegeEscalation: false
  capabilities:
    drop:
      - ALL

# -- Pod security context
podSecurityContext: {}

# -- Node selector for pod assignment
nodeSelector: {}
# -- Tolerations for pod assignment
tolerations: []
# -- Affinity rules for pod assignment
affinity: {}

# -- Additional pod annotations
podAnnotations: {}
# -- Additional pod labels
podLabels: {}

# -- Service configuration
service:
  # -- Service type
  type: ClusterIP
  # -- Service port
  port: 5000
  # -- Service annotations
  annotations: {}
  # -- Service labels
  labels: {}

# -- Ingress configuration
ingress:
  # -- Enable ingress
  enabled: false
  # -- Ingress class name
  className: ""
  # -- Ingress annotations
  annotations: {}
  # -- Ingress hosts
  hosts: []
  #  - host: openvoxview.example.com
  #    paths:
  #      - path: /
  #        pathType: Prefix
  # -- Ingress TLS configuration
  tls: []
  #  - secretName: openvoxview-tls
  #    hosts:
  #      - openvoxview.example.com
